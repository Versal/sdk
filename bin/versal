#!/usr/bin/env node

'use strict';

var argv = require("optimist")
  .usage("Versal Gadget SDK. Usage: versal (create | init | validate | preview | publish) [-arguments] [--options]")
  .demand(1)
  .argv;

require("../lib/sdk").exec(argv._.shift(), argv);