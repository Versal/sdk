(function() {

  define(['helpers/helpers', 'helpers/fixtures', 'views/gadget_instance'], function(Helpers, Fixtures, GadgetInstanceView) {
    beforeEach(function() {
      return this.model = new GadgetInstanceView;
    });
    return describe('Gadget View', function() {});
  });

}).call(this);
